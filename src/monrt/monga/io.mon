# Reads a line of input from the standard input stream.
extern function Input(): string;

# Prints a 32-bit integer to stdout.
extern function PrintInteger(i: int);

# Prints a 64-bit float to stdout.
extern function PrintFloat(f: float);

# Prints a string to stdout.
extern function PrintString(s: string);